load("@rules_rust//rust:defs.bzl", "rust_test")

rust_test(
    name = "binary_tree_test",
    srcs = ["binary_tree.rs"],
    crate_root = "binary_tree.rs",
    edition = "2018",
    deps = [
        # ...list of dependencies if any...
    ],
    use_libtest_harness = True,
)

