load("@rules_rust//rust:defs.bzl", "rust_binary", "rust_test")

rust_binary(
    name = "expressions",
    srcs = ["expressions.rs"],
    crate_root = "expressions.rs",
    edition = "2018",
    deps = [
        # Add dependencies here
    ],
)
