load("@rules_rust//rust:defs.bzl", "rust_test")

rust_test(
    name = "iterator_method_chaining_test",
    srcs = ["iterator_method_chaining.rs"],
    edition = "2018",
    deps = [],
    use_libtest_harness = True,
)
